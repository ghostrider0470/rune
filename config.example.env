# Optional provider credentials for docker-compose.zero-config.yml
# Copy to .env before `docker compose -f docker-compose.zero-config.yml up --build -d`
# Leave keys blank if you want Rune to auto-detect a local Ollama instance instead.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OLLAMA_HOST=
