# Optional provider credentials for the bundled zero-config docker-compose.yml
# Copy to .env before `docker compose up --build -d`
# Leave keys blank if you want Rune to auto-detect a local Ollama instance instead.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OLLAMA_HOST=
