# Memory Bank

Durable project knowledge that should be queryable from inside Rune.

## Layout

- `adr/` — architecture decision records grounded in shipped Rune behavior

## Usage

- use `memory_bank_list` to discover available documents
- use `memory_bank_get` to read a specific document under `memory-bank/`
